import { notImplemented } from "@/app/api/stubs";

export function GET() {
  return notImplemented("Per-shot generation via Shot Router");
}

export function POST() {
  return notImplemented("Per-shot generation via Shot Router");
}
