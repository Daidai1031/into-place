import { notImplemented } from "@/app/api/stubs";

export function GET() {
  return notImplemented("Server-side contribution intake (uploads live in browser storage for now)");
}

export function POST() {
  return notImplemented("Server-side contribution intake (uploads live in browser storage for now)");
}
